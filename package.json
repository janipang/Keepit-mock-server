{
  "name": "keepit-server",
  "version": "1.0.0",
  "description": "node.js mock server to serve example data to keepit frontend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "janipang",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
